Random rand = new Random();
int array = new int[n];
Console.WriteLine("последовательность:");
for (int i = 0; i < n; i++)
{
    array i = rand.Next(0, 9);
    array = Convert.ToInt32(Console.ReadLine());
}
Console.WriteLine(" ");
Console.WriteLine("мешаем массив:");
var random = new Random();
for (var i = 0; i < n; i++)
{
    var key = array j;
    var rnd = random.Next(j, array.Length);
    array i = array j;
    array j = key;
}
for (int i = 0; i < n; i++)
{
    Console.Write(array i);
}
